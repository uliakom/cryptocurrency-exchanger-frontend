import styled from '@emotion/styled';
import { Box } from '@mui/material';

export const BoxOperationListStyled = styled(Box)`
  padding: 4px;
  color: ${({ theme }) => theme.colors.primary};
  background-color: transparent;
`;
